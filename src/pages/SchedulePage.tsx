export function SchedulePage() {
  return (
    <section>
      <h2>Schedule</h2>
      <p>Run the deterministic solver and view/persist the resulting schedule.</p>
      <p className="muted">Solver + DB wiring lands in a follow-up step.</p>
    </section>
  )
}